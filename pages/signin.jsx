import {ContentPageHeader} from '../components/headers'

function SignInPage() {
    return (  
        <ContentPageHeader
        type="Sign in to your account"
        title="Sign in service"
        tagline="Will add a sign in form"
        
        />
    );
}

export default SignInPage;